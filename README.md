echochoice/
├── public/
│   └── index.html
├── package.json
├── vercel.json
└── README.md

Deploy: git init && git add . && git commit -m "EchoChoice MVP" && git remote add origin <repo-url> && git push -u origin main
Vercel: New Project → Import GitHub repo → Framework Preset: Other → Root Directory: / → Deploy
